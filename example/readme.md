# VoIP-over-MQTT demo project

Here you can find an example of the VoIP addon working with MQTT.

## Before launching

Here is an example without native libraries. Before launching the project, copy the `twovoip` addon here.
